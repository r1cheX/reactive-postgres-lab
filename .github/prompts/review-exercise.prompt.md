---
name: review-exercises
description: Review current code against the pending exercises in reactive-postgres-lab
---

Find the pending exercises in `LAB_PROGRESS.md` (status `pending`).

Review the current code changes against that exercise's goal:
- Give a brief pass/fail verdict.
- If fail or partial: up to 3 short, concrete pointers (no rewrite unless explicitly asked).
- If pass: mark the exercise `done` in `LAB_PROGRESS.md` with today's date, and state readiness for `/next-exercise`.

Keep the whole review under ~8 lines unless the user asks for more detail.
